import { FieldClientCreateScreen } from "@/features/clients";

// Alta de cliente en campo (pantalla 17c). Dentro del shell: muestra el tab bar
// (pestaña Clientes activa) + su propio hero de gradiente.
export default function CollectorNewClientPage() {
  return <FieldClientCreateScreen />;
}

import { RouteDetailScreen } from "@/features/cobros";

// DESIGN_SYSTEM.md §3.5 — Detalle de ruta (#15c): resumen del día + clientes
// pendientes/cobrados de la ruta seleccionada en "Mis rutas".
export default function CollectorRouteDetailPage() {
  return <RouteDetailScreen />;
}

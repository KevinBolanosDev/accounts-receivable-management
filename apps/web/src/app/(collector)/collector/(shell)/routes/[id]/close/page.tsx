import { CloseRouteScreen } from "@/features/closures";

// DESIGN_SYSTEM.md §4.6 — Cierre de ruta (#19c), anidada bajo el detalle de
// ruta del cobrador ("Cerrar ruta" en el header de #15c).
export default function CollectorCloseRoutePage() {
  return <CloseRouteScreen />;
}

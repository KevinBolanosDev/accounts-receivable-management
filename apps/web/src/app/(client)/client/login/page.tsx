import { ClientLoginScreen } from "@/features/client-auth";

// Pantalla #20c v2 — login con credenciales del cliente final.
export default function ClientLoginPage() {
  return <ClientLoginScreen />;
}
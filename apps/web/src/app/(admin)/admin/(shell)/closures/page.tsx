import { ClosuresHistoryScreen } from "@/features/closures";

// DESIGN_SYSTEM.md §3.12 — Histórico de cierres (#12c).
export default function AdminClosuresPage() {
  return <ClosuresHistoryScreen />;
}
